{
  "name": "opcode-api",
  "private": true,
  "version": "0.2.1",
  "license": "AGPL-3.0",
  "type": "module",
  "scripts": {
    "build": "cd src-tauri && cargo build --release",
    "dev": "cd src-tauri && cargo run -- api",
    "check": "cd src-tauri && cargo check"
  },
  "description": "API-only build of Opcode - HTTP API server for Claude Code Agents"
}